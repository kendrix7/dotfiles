#!/bin/bash

# Script to list all custom aliases and scripts with descriptions
# Auto-generated by update_shortcuts.sh

echo "========================================="
echo "       MY DEVELOPMENT SHORTCUTS"
echo "========================================="
echo ""
echo "📁 PROJECT NAVIGATION:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "🔀 GIT SHORTCUTS:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "🐳 DOCKER SERVICES:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "🔑 TOKEN GENERATION:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "📦 NPM SHORTCUTS:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "🛠️  UTILITY SCRIPTS:"
  cdpui        - Jump to cdp-ui directory
  cdpbe        - Jump to cdp-behind-service directory
  work        - Jump to work directory
  compose        - Jump to docker-compose directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  redis-up        - Jump to Redis && docker-compose up -d directory
  redis-down        - Jump to Redis && docker-compose down directory
  postgres-up        - Jump to Postgres && docker-compose up -d directory
  postgres-down        - Jump to Postgres && docker-compose down directory
  kafka-up        - Jump to Kafka && docker-compose up -d directory
  kafka-down        - Jump to Kafka && docker-compose down directory
  token-local        - Jump to admin token --env local directory
  token-dev        - Jump to admin token --env dev directory
  token-stage        - Jump to admin token --env stage directory
  token-prod        - Jump to admin token --env prod directory
  dotfiles        - Jump to dotfiles directory
  gs           - git status
  ga           - git add
  gc           - git commit -m
  gp           - git push
  gl           - git pull
  gco           - git checkout
  gb           - git branch
  gd           - git diff
  glog           - git log --oneline --graph
  gcp 'msg'    - git add . && git commit -m 'msg' && git push (all-in-one)
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  redis-up         - Start redis container
  redis-down       - Stop redis container
  postgres-up         - Start postgres container
  postgres-down       - Stop postgres container
  kafka-up         - Start kafka container
  kafka-down       - Stop kafka container
  dev-services-up         - Start dev-services container
  dev-services-down       - Stop dev-services container
  token-local      - Generate token for local environment
  token-dev      - Generate token for dev environment
  token-stage      - Generate token for stage environment
  token-prod      - Generate token for prod environment
  nrs          - npm run start
  nrt          - npm run test
  nrtu          - npm run test:unit
  nrdn          - npm run dev:nodb
  update-shortcuts - Auto-update shortcuts list and push to git
  shortcuts    - Display this help menu
  reload       - Reload shell configuration
  killport 3000 - Kill process on port 3000echo ""
echo "💡 TIPS:"
echo "  - All aliases are defined in ~/.zshrc"
echo "  - Run 'source ~/.zshrc' after editing shell config"
echo "  - Use 'which aliasname' to see what an alias does"
echo ""

# Show current environment status
echo "🔍 CURRENT STATUS:"
if [ -n "$NPM_TOKEN" ]; then
    echo "  ✅ NPM_TOKEN is set and ready"
else
    echo "  ❌ NPM_TOKEN not found"
fi

if command -v volta >/dev/null 2>&1; then
    echo "  ✅ Volta (Node manager) installed: $(volta --version)"
else
    echo "  ❌ Volta not found"
fi

if command -v code >/dev/null 2>&1; then
    echo "  ✅ VS Code CLI available"
else
    echo "  ❌ VS Code CLI not available"
fi

if command -v docker >/dev/null 2>&1; then
    echo "  ✅ Docker installed: $(docker --version | cut -d' ' -f3 | cut -d',' -f1)"
else
    echo "  ❌ Docker not installed"
fi

echo ""
echo "Run individual commands to test them out!"
